apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: loki
  namespace: argocd
spec:
  project: default
  sources:
    # Upstream Loki chart (SingleBinary mode via values)
    - repoURL: https://grafana.github.io/helm-charts
      chart: loki
      targetRevision: "6.55.0"
      helm:
        valueFiles:
          - $values/deploy/charts/loki/values.yaml
    # Values file from this repo
    - repoURL: https://github.com/ghdtjdwn/ssuMCP.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
