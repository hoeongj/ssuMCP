apiVersion: v1
kind: Service
metadata:
  name: ssuai-backend
  namespace: ssuai-prod
  labels:
    app.kubernetes.io/name: ssuai-backend
    app.kubernetes.io/part-of: ssuai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ssuai-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
