# Smithery MCP 서버 등록 설정
# https://smithery.ai 에서 이 파일을 읽어 서버 연결 방식을 파악합니다.

startCommand:
  type: http
  configSchema:
    type: object
    properties: {}
    required: []
    additionalProperties: false
  exampleConfig: {}
  url: https://ssumcp.duckdns.org/mcp
