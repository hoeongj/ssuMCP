package com.ssuai.domain.library.reservation.intent;

public enum LibraryReservationIntentEventType {
    WAIT_REGISTERED,
    SEAT_FOUND,
    RESERVATION_SUCCEEDED,
    RESERVATION_FAILED,
    CANCELLED,
    EXPIRED
}
